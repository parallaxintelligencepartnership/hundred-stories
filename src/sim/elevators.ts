import type { Id, World } from './types';
export const IDLE_RETURN_MINUTES = 10;
export function requestHallCall(_world: World, _shaftId: Id, _floor: number, _dir: 1 | -1): void {}
export function tickElevators(_world: World): void {}
