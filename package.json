{
  "name": "hundred-stories",
  "version": "0.1.0",
  "description": "A downtown skyscraper simulation: run and operate a tower, one hundred stories tall.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "deploy:pages": "npm run build && npx wrangler@latest pages deploy dist --project-name hundred-stories"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "pixi.js": "8.21.0"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vite-plugin-pwa": "1.3.0",
    "vitest": "5.0.1"
  }
}
