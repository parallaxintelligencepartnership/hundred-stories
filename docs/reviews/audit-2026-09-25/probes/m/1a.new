  const paid = spend(world, rule.cost, rule.label);
  if (!paid.ok) return no(cannotAfford(rule.label, rule.cost));
  spend(world, rule.cost, rule.label);

  const room = makeRoom(world, kind, floor, x);
