node_modules/
dist/
dev-dist/
.env
.env.*
.DS_Store
*.log
coverage/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
.claude/worktrees/

# Capacitor app bundle (npm run build:app); ios/ and android/ carry their own .gitignore for native build outputs
dist-app/

# Tauri build output (src-tauri/.gitignore also covers it)
src-tauri/target/

# Store screenshots and graphics (npm run store:shots)
store/shots/

# Signing material and store ids never enter the repo (store/SUBMISSION.md keeps them in ~/keys and the keychain)
*.jks
*.keystore
*.p12
*.p8
*.mobileprovision
steam_appid.txt
